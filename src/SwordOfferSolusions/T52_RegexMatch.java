package SwordOfferSolusions;

public class T52_RegexMatch {
    public boolean match(char[] str, char[] pattern)
    {
        if(str.length == 0 || pattern.length == 0)
            return false;
        int i = 0;
        int j = 0;
        while(i < pattern.length){
            while(j<str.length){
//                if(pattern[i] == ".")

            }
        }
        return true;
    }
}
